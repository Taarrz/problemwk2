#include <stdio.h>

int main() {

    int n, m, GCD;
    int i=1 ;
    scanf("%d %d", &n, &m);
    while(i<=n && i<=m){
        if(n % i == 0 && m % i == 0){
            GCD = i;
            }
            i=i+1;
    }

    printf("lcm17 = %d", n*m/GCD);

    return 0;
}
